# FastAPI Backend URL for the frontend to connect to
# NOTE: With Next.js rewrites enabled, this is optional. Requests to /api/* are automatically proxied.
# Only set this if you need to override the default behavior.
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8888

# Node environment
NODE_ENV=production
